name: crosmos
version: 1.0.0
description: "Crosmos memory provider — persistent memory for Hermes agents with auto-recall and auto-ingest."
hooks:
  - prefetch
  - sync_turn
  - on_session_end
pip_dependencies:
  - httpx
